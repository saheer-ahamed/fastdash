//! Incremental JSONL parser for `~/.claude/projects` session transcripts.
//!
//! TODO(feat/claude): a `notify`-based watcher that reads only bytes appended
//! to the active session file and feeds a running aggregate, plus a cold
//! full-scan on startup.
